#include <stdio.h>

int main(void) {
  int a = 3 + 4;
	int b = (5*a - 3) + 21;
	printf("The value that a holds is: %d\n", a);
	printf("The value that b holds is: %d\n", b);
	printf("What does a + b equal to? %d\n", a + b);
	return 0;
}
